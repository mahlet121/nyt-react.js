var React = require('react');
var ReactDOM = require('react-dom');

var routes = require('./config/routes');
// To display the result take routes
ReactDOM.render(routes, document.getElementById('root'));
